<h1>Productos:</h1>
<!-- Hago un ciclo para recorrer todos los datos de la BD -->
<% @products.each do |product| %>
    <%= link_to product_path(product.id) do %>
    <h2><%= product.tittle %></h2>
    <% end %>
    <span><%= product.price %></span>
<% end %>